services:
  app:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        BASE_HREF: ${BASE_HREF:-/}
    container_name: neural-network-platform
    restart: unless-stopped
