services:
  app:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      target: production
      args:
        BASE_HREF: ${BASE_HREF:-/}
    image: neural-network-development-platform:local
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
